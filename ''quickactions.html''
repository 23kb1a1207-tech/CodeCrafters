import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Link } from "react-router-dom";
import { createPageUrl } from "@/utils";
import { Plus, CheckSquare, DollarSign, BarChart3, Calendar } from "lucide-react";

export default function QuickActions() {
  const actions = [
    {
      title: "Add New Task",
      description: "Create a task with budget tracking",
      icon: CheckSquare,
      color: "bg-blue-500 hover:bg-blue-600",
      url: createPageUrl("Tasks")
    },
    {
      title: "Log Expense",
      description: "Record a new budget entry",
      icon: DollarSign,
      color: "bg-green-500 hover:bg-green-600", 
      url: createPageUrl("Budget")
    },
    {
      title: "View Reports",
      description: "Analyze spending patterns",
      icon: BarChart3,
      color: "bg-purple-500 hover:bg-purple-600",
      url: createPageUrl("Budget")
    }
  ];

  return (
    <Card className="bg-white/80 backdrop-blur-sm border border-white/20 shadow-lg">
      <CardHeader>
        <CardTitle className="text-xl font-bold">Quick Actions</CardTitle>
      </CardHeader>
      <CardContent>
        <div className="space-y-3">
          {actions.map((action, index) => (
            <Link key={index} to={action.url} className="block">
              <Button 
                className={`w-full justify-start gap-3 h-auto p-4 ${action.color} text-white`}
                variant="default"
              >
                <action.icon className="w-5 h-5" />
                <div className="text-left">
                  <p className="font-medium">{action.title}</p>
                  <p className="text-sm opacity-90">{action.description}</p>
                </div>
              </Button>
            </Link>
          ))}
        </div>
      </CardContent>
    </Card>
  );
}